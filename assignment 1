#include <stdio.h>
	void printTable(int num, int i)
{
	if (i > 10 )
return;
printf("%d x %d = %d\n", num ,i ,num*i);
printTable(num,i+1);
}
int main ()
{
int num;
printf("Enter a number: ");
scanf("%d", &num);
printTable(num,1);
return 0;

}
